#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a, b, c, delta, x1, x2;
    cout << "Imetti a, b, c (es. 1 -3 2): "
    cin >> a >> b >> c;
    delta = pow(b,2)-4*a*c
    x1 = ( (-b) - sqrt(delta) ) / (2*a);
    x2 = ( (-b) + sqrt(delta) ) / (2*a);
    cout << "Soluzione (es.)"

    return 0;
}